// Calculo IMC
const nome = 'Gabriel'
const peso = 65
const altura = 1.6

const IMC = peso / (altura * altura)
if (IMC >= 30) {
  console.log(`${nome} você está acima do peso`)
} else {
  console.log(`${nome} você está acima do peso`)
}